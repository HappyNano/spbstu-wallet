EXECUTABLE(linux-main)

SRCS(
  ${PROJECT_SOURCE_DIR}/platforms/sdl/main.cpp
)

LIBS(
  imgui::imgui
  ${SDL}

  # EGL
  # GLESv3
)

END()

target_compile_definitions(linux-main PRIVATE
IMGUI_IMPL_OPENGL_ES3
)
